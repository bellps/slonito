<%= render(::DatabaseNavigationComponent.new(chats: @chats)) %>
<%= turbo_frame_tag 'notifications' %>

<main class="md:ml-64 flex flex-col h-screen">
  <div id="chat-parent" class="flex flex-col flex-1 h-screen">
    <%= render(::ChatComponent.new) %>
  </div>
  <%= render(::NewChatModalComponent.new) %>
</main>
