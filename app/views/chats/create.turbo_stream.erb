<%= turbo_stream.before 'chat_window' do %>
  <script>
    history.pushState(null, "", "<%= chat_path(@chat) %>");
    document.getElementById("chats").reload()
  </script>
<% end %>

<%= turbo_stream.replace 'chat_window', ::ChatComponent.new(chat: @chat) %>
